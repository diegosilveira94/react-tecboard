// import { themes } from "./themes";

export interface Event {
  id: string;
  src: string;
  alt: string;
  theme: string;
  date: Date;
  title: string;
  description: string;
}

export const events = [{}];
